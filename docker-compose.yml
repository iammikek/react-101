services:
  web:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    environment:
      - VITE_API_URL=http://host.docker.internal:8000
    extra_hosts:
      - 'host.docker.internal:host-gateway'
